@import "mixins.styl"
@import "reset.styl"
@import "animation.styl"
@import "statPanel.styl"

body 
  font-family: 'Roboto', sans-serif
  background-color $mainBackgroundColor
  font-size 14px
  margin 1em
  color $mainFontColor
.container 
  margin 0 auto
  min-height 100%
.appHeader 
  background $altBackgroundColor
  padding 2em 3em
  text-align center
h1 
  font-size 2.5em
  display block
  margin-right 1em
